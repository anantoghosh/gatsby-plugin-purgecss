language: node_js

node_js:
  - "lts/*"

before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version 1.6.0
  - export PATH=$HOME/.yarn/bin:$PATH

script:
  - yarn test
  - yarn build

cache:
  yarn: true
  directories:
    - "node_modules"
